manifest.mainAttributes(
    'Implementation-Title': 'Geology Models',
    'Implementation-Version': version
)

dependencies {
    compile project(":coretools")
    compile project(":coretools-model")
}