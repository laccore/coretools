manifest.mainAttributes(
    'Implementation-Title': 'DIS-specific Models',
    'Implementation-Version': version
)

dependencies {
    compile project(":coretools")
    compile project(":coretools-model")
    compile project(":geology-base")
}